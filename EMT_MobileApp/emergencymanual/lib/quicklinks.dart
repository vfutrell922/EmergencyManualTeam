import 'package:flutter/material.dart';

class QuickLinksPage extends StatelessWidget {
  @override
  Widget build(BuildContext ctxt) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Quick Links"),
      ),
      body: new Text("Quick links...!!!!!!"),
    );
  }
}
