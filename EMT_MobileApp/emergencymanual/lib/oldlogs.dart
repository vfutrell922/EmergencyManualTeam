import 'package:flutter/material.dart';

class OldLogsPage extends StatelessWidget {
  @override
  Widget build(BuildContext ctxt) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Old Logs"),
      ),
      body: new Text("Old Logs Page...!!!!!!"),
    );
  }
}
